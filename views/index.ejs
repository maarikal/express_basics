<!doctype html>

<head>
    <link rel="stylesheet" href="/style.css">
    <title> Hello page </title>
    </head>

<body>
    <h2> Questions about express </h2>
    <% questions.forEach((question) => { %>
        <h3> <%= question['title'] %> </h3>
        <p> <%= question['user'] %></p>
        <p> <%= question['votes'] %></p>
    <% }); %>


</body>
</html>